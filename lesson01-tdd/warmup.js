// Warm-up Exercises: JavaScript Array Methods

// 1. Create a function that uses Array.prototype.map
// Convert an array of numbers to their squared values
function squareNumbers(numbers) {
    // Your code here
}

// 2. Create a function that uses Array.prototype.filter
// Filter out all negative numbers from an array
function removeNegatives(numbers) {
    // Your code here
}

// 3. Create a function that uses Array.prototype.reduce
// Calculate the sum of all numbers in an array
function calculateSum(numbers) {
    // Your code here
}

// Sample usage:
const numbers = [1, -2, 3, -4, 5];
console.log(squareNumbers(numbers));  // Should output: [1, 4, 9, 16, 25]
console.log(removeNegatives(numbers)); // Should output: [1, 3, 5]
console.log(calculateSum(numbers));    // Should output: 3