// Warm-up Exercises: Bit Manipulation

// 1. Create a function to count set bits in a number
function countSetBits(n) {
    // Your code here
}

// 2. Create a function to check if a number is a power of 2
function isPowerOfTwo(n) {
    // Your code here
}

// 3. Create a function to find the single number that appears once
// while all other numbers appear twice
function findSingleNumber(arr) {
    // Your code here
}

// Sample usage:
console.log(countSetBits(7));     // Should output: 3 (111 in binary)
console.log(isPowerOfTwo(8));     // Should output: true
console.log(isPowerOfTwo(6));     // Should output: false
console.log(findSingleNumber([4,1,2,1,2]));  // Should output: 4
