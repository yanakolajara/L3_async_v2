/**
 * Design a HashMap without using any built-in hash table libraries.
 * 
 * Implement the MyHashMap class:
 * - put(key, value) inserts a (key, value) pair into the HashMap
 * - get(key) returns the value to which the key is mapped, or -1 if no mapping exists
 * - remove(key) removes the key and its value if the map contains the mapping for the key
 */
class MyHashMap {
    constructor() {
        // Initialize your data structure here
    }
    
    /**
     * Value will always be non-negative.
     * @param {number} key
     * @param {number} value
     * @return {void}
     */
    put(key, value) {
        // Your code here
    }
    
    /**
     * Returns the value to which the specified key is mapped, or -1 if this map contains no mapping for the key
     * @param {number} key
     * @return {number}
     */
    get(key) {
        // Your code here
    }
    
    /**
     * Removes the mapping of the specified value key if this map contains a mapping for the key
     * @param {number} key
     * @return {void}
     */
    remove(key) {
        // Your code here
    }
}

module.exports = MyHashMap;
